<h1>Song List</h1>

<table>
	<tbody>
<% @songs.each do |song| %>
	<tr><%= link_to song.title, song_path(song) %></tr>
<% end %><br>
	</tbody>
</table>
<%= link_to "Create a Song", new_song_path %> 
</div>